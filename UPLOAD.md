# Publish the updated ticket-lifecycle README

Destination: https://github.com/jmccuf/ticket-lifecycle

1. Download and extract `ticket-lifecycle-visuals.zip`.
2. Review the README and screenshots. The original screenshots retain lab account names and timestamps; redact them before uploading if needed.
3. In the repository, replace the root `README.md` with this package's `README.md`.
4. Upload the complete `images` folder alongside README.md, preserving the filenames and directory structure.
5. Commit the changes, then view the repository homepage and check that all five images display.

Do not paste only the Markdown without uploading the image files. The relative links require an `images` folder in the same directory as README.md.

## Package contents

- README.md — expanded walkthrough with evidence-based captions and optional cybersecurity exercises.
- images/ticket-lifecycle.svg — explanatory workflow graphic.
- images/security-handoff.svg — recommended security-aware handling practices.
- images/ticket-queue.png — original intake and assignment thread.
- images/ticket-response.png — original investigation note and closure event.
- images/ticket-closure.png — original closed-ticket queue.

No changes have been made to your GitHub repository. The added recommendations have not been tested against a live osTicket deployment.
